/**
 * Класс для представления кредитного счета.
 */
public class CreditAccount extends Account {
    /**
     * Конструктор кредитного счета.
     * @param initialBalance Начальный баланс кредитного счета.
     */
    public CreditAccount(double initialBalance) {
        super(initialBalance);
    }
}